{
  "name": "bunny-boo-testbed",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "assets": "node tools/build-assets.mjs",
    "build:itch": "node build.js itch",
    "test": "playwright test",
    "serve": "npx serve ."
  },
  "devDependencies": {
    "@playwright/test": "^1.47.0"
  }
}